{
  // Nuxt generates the real tsconfigs under .nuxt/ — this file just references
  // them. Add project-wide compiler tweaks in nuxt.config.ts `typescript.tsConfig`.
  "files": [],
  "references": [
    { "path": "./.nuxt/tsconfig.app.json" },
    { "path": "./.nuxt/tsconfig.server.json" },
    { "path": "./.nuxt/tsconfig.shared.json" },
    { "path": "./.nuxt/tsconfig.node.json" }
  ]
}
